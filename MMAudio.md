# MMAudio：Taming Multimodal Joint Training for High-Quality Video-to-Audio Synthesis

## 示意图

![img](/Users/cbhao/dev/pars/MMAudio.assets/(null))

## 贡献点

- 提出了联合训练，使得统一特征性空间，将模型暴露在大量数据上。显著提高音频质量和语义对齐。

![image-20250416101119576](/Users/cbhao/dev/pars/MMAudio.assets/image-20250416101119576.png)

- 为了增加时间上的对齐，conditional synchronization module

## 基础知识

1. V2A 中很注重高质量以及同步性
2. 多模态学习并不会妨碍单模态上的能力
3. **Foley 音效合成**，也就是为一段给定视频合成出**环境音**（如雨声、河水流动声）以及由视频中可见事件引发的**音效**（如狗叫、网球拍击球的声音）。不包括背景音乐或人声，这些通常是在后期制作中添加的。
4. 现有的 Vedio-Audio 数据有限，VGGSOUND（550H），互联网上的视频具有人声，背景音乐以及后期添加的并不属于原视频的音效
5. 传统训练方式：从头训练 video2Audio ；或者是从 text2audio 增加控制模块进行过度（增加复杂性同时设计的点子不多，提升效果有限）

**语义对齐（Semantic Alignment）**

现有研究主要通过两种方式在音频和视频之间学习语义对齐：

- **生成式目标训练**（如 [2, 6, 18, 44, 49, 60, 66, 71]）
- **对比学习目标**（如 [42]）

本文进一步引入了**音频-文本配对数据**进行训练，认为通过联合训练音频-文本和视频-文本对，可以构建**共享的语义空间**（类似 ImageBind 和 LanguageBind），从而获得更丰富的语义理解能力。

**时间对齐（Temporal Alignment）**

除了使用音视频配对数据直接学习时间对齐，一些方法也训练模型预测手工设计的代理特征（如音频起点、能量、均方根等 [4, 17, 20, 34, 54, 73]）。
本文则采用**预训练的自监督去同步检测器 Synchformer [19]** 的深度特征嵌入来直接学习对齐信息，避免了手工特征设计。
V-AURA [65] 也使用了 Synchformer，但仅用于自回归生成，且没有进行文本多模态训练，使用的上下文窗口也较短（2.56s vs 本文的8-10s）。

在**位置编码改进方面**，Mei 等人 [45] 提出对高频（音频）位置编码进行下采样，而本文则对低频（视频）位置编码进行频率提升，实质效果相同（频率成整数倍时）。

多模态条件（Multimodal Conditioning）

常见方法是将“控制模块”嵌入预训练的文本到音频模型中，引入视觉特征 [13, 17, 20, 34, 47, 54, 73]，但这会增加参数量，且固定文本条件后，不利于学习真正的**联合语义空间**。

本文不同之处在于：**所有模态同时训练**，支持模态间的全向特征共享，有助于更好地学习语义绑定。

另有方法如 Seeing-and-Hearing [69]，在测试时通过梯度上升优化对齐分数（类似 ImageBind），但存在测试慢、输出质量差的问题。
相比之下，本文方法测试更快，生成结果更稳定。

VATT [40] 和 MultiFoley [3] 与本文工作并行进行，也探索了多模态训练。其中，MultiFoley 结构类似，但视觉帧率只有 8 FPS，而本文使用的是 **24 FPS**，因此在音视频同步效果上更优。

------

### 4. 多模态生成（Multimodal Generation）

这类方法旨在生成包含多种模态的数据（如音频+视频），但目前尚不如专门的视频转音频模型 [25, 55, 61, 62] 效果好。
本文聚焦于**多模态条件下的音频生成**，其提出的多模态训练框架和架构也为未来的多模态生成工作打下了基础。

## 模型结构

- CFM
- VAE
- vocoder

音频数据 - stft -> mel - vae encoder -> vae_latent -> vae decoder -> mel - vocoder -> wav

## Multimodal Transformer



## Tricks

## 试验

## 评测