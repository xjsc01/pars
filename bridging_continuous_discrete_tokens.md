# Bridging Continuous and Discrete Tokens

> 

## 示意图



## 贡献点

1. 将 VAE 的训练与离散 tokens 分离开来，根据概率分布来量化，保留模型的表达能力。【先训练一个 VAE，然后根据 VAE 的分布，划分 tokens】
2. 对于每一个 dimension 进行独立建模。
3. 在预测的过程中，对于一个时间步（语音）上的 token 分别进行预测，而不是一次性预测所有维度的 token，是分开了进行预测的。

## 模型的好处

1. 避免了训练过程中使用 token 造成的不稳定性。
2. 码本大小可以任意选择
3. 尽可能地保留了分布的特性

## 基础知识

1. 将连续表征转化为离散表征之后可以直接使用大模型预测 tokens ce_loss，但是信息保留和稳定性不足
2. 预测 token 的方式和 LLM 类似，便于进行多模态，以及在 LLM 中涌现出的缩放技术以及创新架构
3. 如果将连续表征进行量化（VQ，LFQ），**首先，**量化本质上不可微，需要梯度近似，导致优化复杂以及不稳定。**其次，**有限的 codebook 保留的信息少，并且过多的 codebook 会导致码本利用率低以及增加建模复杂度。
4. 连续表征的 LLM：diffusion head or Gaussian Mixture Models

## 模型结构

细粒度的离散化会导致一个指数级增长的 token 空间，使得直接进行分类预测变得计算成本极高。（token 空间太大，分类器训练&推理都困难）

为了解决这个问题，我们提出了一种轻量的**逐维自回归机制**，将 token 的预测过程分解为一个个维度上的预测序列。（不是直接预测一个超大的 token，而是逐维预测每个数值）

1. 保持维度直接的重要依赖关系
2. 避免了大词汇量带来的计算负担



## Multimodal Transformer

## Tricks

## 试验

## 评测